apiVersion: v1  
kind: PersistentVolumeClaim  
metadata:  
  name: postgres-pv-claim  
  namespace: insource1
spec:
  volumeName: postgres-pv-001
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi 