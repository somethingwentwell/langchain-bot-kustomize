apiVersion: v1  
kind: PersistentVolumeClaim  
metadata:  
  name: postgres-pv-claim  
  namespace: insource004
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: postgres-pv-004
  resources:
    requests:
      storage: 1Gi