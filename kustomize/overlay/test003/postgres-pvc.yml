apiVersion: v1  
kind: PersistentVolumeClaim  
metadata:  
  name: postgres-pv-claim  
  namespace: insource003
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: postgres-pv-003
  resources:
    requests:
      storage: 1Gi